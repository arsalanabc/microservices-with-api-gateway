import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
    flex: 1;
    margin-right: 1rem;
`;

const Content = () => {
    return(
        <Wrapper>this is from content</Wrapper>
    );
};

export default Content;